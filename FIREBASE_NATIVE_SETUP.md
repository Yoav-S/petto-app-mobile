# Native Firebase config

Canonical package: **com.yoav.petto** (Firebase project **petto-494013**).

Files in this folder:
- `google-services.json` — Android
- `GoogleService-Info.plist` — iOS

See `../server/docs/firebase-package-alignment.md` for RevenueCat Analytics App IDs and build notes.
